# spring:
#   datasource:
#     url: ${DB_URL}
#     username: ${DB_USERNAME}
#     password: ${DB_PASSWORD}
#     driver-class-name: com.mysql.cj.jdbc.Driver

#   jpa:
#     hibernate:
#       ddl-auto: update
#     show-sql: true

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/english_learning_db
    username: root
    password: thai2004
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
